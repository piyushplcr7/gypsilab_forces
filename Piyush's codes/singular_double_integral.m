% Evaluation of singular double integrals
mesh = 
Omega = dom(mesh,3);
S1_Omega = fem(mesh,'P1');

N_elt = size(mesh.elt,1);
N_rsf = 

% Skeleton for panel oriented assembly
for i = 1:N_elt
   for j = 1:N_elt
      % Assembly for panel 
       
   end
end
